{
  "_comment_phase5": "PHASE 5 ADDITIONS — splice the PreCompact and SessionStart blocks below into the existing settings.template.json's `hooks` object. Order: keep Stop array first (Phase 1), then PostToolUse (Phase 3, when added), then PreCompact, then SessionStart. The `_comment_phase5` key itself is illustrative — strip when merging.",

  "hooks": {
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PROJECT_DIR/.claude/hooks/pre-compact.sh"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PROJECT_DIR/.claude/hooks/post-compact-restore.sh"
          }
        ]
      }
    ]
  }
}
